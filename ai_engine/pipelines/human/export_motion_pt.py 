from ultralytics import YOLO

# Load latest SOTA pretrained YOLO11 nano/small for real-time motion (person detection triggers alerts)
model = YOLO('yolo11n.pt')  # Or 'yolo11s.pt' for higher accuracy

# Fine-tune optionally on custom surveillance dataset (e.g., for loitering)
# model.train(data='custom_motion.yaml', epochs=50)

# Export to PyTorch (default) or optimize
model.save('../models/human/smart_motion.pt')  # Saves as .pt

print("Exported smart_motion.pt – SOTA YOLO11 for real-time motion/person detection (mAP 56+)")
