# Minimal notifier placeholder. Expand to Twilio / Firebase later.
def send_push(user_id: str, title: str, body: str):
    # placeholder - integrate firebase cloud messaging or twilio
    print(f"[Notifier] To:{user_id} - {title}: {body}")
    return True

